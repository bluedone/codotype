import { Meta, Story } from "@storybook/addon-docs";

<Meta title="Docs/Configuration/Configuration Property/String Property Transformations" />

# String Property Transformations

The **String Property Transformations** do....

### The `lowercase` transform

The `lowercase` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--lowercase" />

### The `uppercase` transform

The `uppercase` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--uppercase" />

### The `titlecase` transform

The `titlecase` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--titlecase" />

### The `camelcase` transform

The `camelcase` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--camelcase" />

### The `snakecase` transform

The `snakecase` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--snakecase" />

### The `pascalcase` transform

The `pascalcase` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--pascalcase" />

### The `kebabcase` transform

The `kebabcase` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--kebabcase" />

### The `no-numbers` transform

The `no-numbers` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--no-numbers" />

### The `no-symbols` transform

The `no-symbols` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--no-symbols" />

### The `trim-whitespace` transform

The `trim-whitespace` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--trim-whitespace" />

### The `remove-whitespace` transform

The `remove-whitespace` transformation ensures the string input does...

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--remove-whitespace" />

## Combining Transformations

String Property Transformations can be combined to...

Snake-case with no symbols:

<Story id="components-projecteditor-configurationeditor-configurationinputprimitive-stringpropertyfilter--no-symbols-snakecase" />
