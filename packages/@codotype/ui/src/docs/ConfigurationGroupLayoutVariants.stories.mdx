import { Meta, Story } from "@storybook/addon-docs";

<Meta title="Docs/Configuration/Configuration Group/Layout Variants" />

# Configuration Group Layout Variants

TODO - document what the `Group Layout Variants` do

### `tabs` Layout

The `tabs` Group Layout Variant is used for...

<Story id="components-projecteditor-configurationeditor-configurationinput-tabs--allowdisable-false" />

### `list` Layout

The `list` Group Layout Variant is used for...

<Story id="components-projecteditor-configurationeditor-configurationinput-list--allowdisable-false" />

### `docs` Layout

The `docs` Group Layout Variant is used for...

<Story id="components-projecteditor-configurationeditor-configurationinput-docs--allowdisable-false" />

### `docs-3x9` Layout

The `docs-3x9` Group Layout Variant is used for...

<Story id="components-projecteditor-configurationeditor-configurationinput-docs-3x9--allowdisable-false" />

### `docs-4x8` Layout

The `docs-4x8` Group Layout Variant is used for...

<Story id="components-projecteditor-configurationeditor-configurationinput-docs-4x8--allowdisable-false" />

### `docs-6x6` Layout

The `docs-6x6` Group Layout Variant is used for...

<Story id="components-projecteditor-configurationeditor-configurationinput-docs-6x6--allowdisable-false" />

### `detail-3x9` Layout

The `detail-3x9` Group Layout Variant is used for...

<Story id="components-projecteditor-configurationeditor-configurationinput-detail-3x9--allowdisable-false" />

### `detail-4x8` Layout

The `detail-4x8` Group Layout Variant is used for...

<Story id="components-projecteditor-configurationeditor-configurationinput-detail-4x8--allowdisable-false" />

### `detail-6x6` Layout

The `detail-6x6` Group Layout Variant is used for...

<Story id="components-projecteditor-configurationeditor-configurationinput-detail-6x6--allowdisable-false" />
